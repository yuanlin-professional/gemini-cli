# docs/mermaid

## 概述

此目录存放项目文档中使用的 Mermaid 图表源文件。Mermaid 是一种基于文本的图表绘制工具，这些 `.mmd` 文件定义了项目架构和流程的可视化图表，用于辅助理解系统设计。

## 目录结构

```
mermaid/
├── context.mmd       # 上下文关系图，展示系统组件之间的上下文交互关系
└── render-path.mmd   # 渲染路径图，展示 CLI 的渲染处理流程
```
