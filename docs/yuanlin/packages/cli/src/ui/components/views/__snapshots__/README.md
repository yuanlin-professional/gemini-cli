# __snapshots__ (packages/cli/src/ui/components/views/)

## 概述

此目录为 `packages/cli/src/ui/components/views/` 下的测试快照目录，用于存储视图组件的测试快照，涵盖聊天列表、MCP 状态和工具列表等视图。

## 目录结构

```
__snapshots__/
├── ChatList.test.tsx.snap   # 聊天列表视图快照
├── McpStatus.test.tsx.snap  # MCP 状态视图快照
└── ToolsList.test.tsx.snap  # 工具列表视图快照
```
