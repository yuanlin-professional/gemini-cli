# __snapshots__ (packages/cli/src/ui/components/SessionBrowser/)

## 概述

此目录为 `packages/cli/src/ui/components/SessionBrowser/` 下的测试快照目录，用于存储会话浏览器组件的测试快照，包括搜索导航和状态显示相关的快照。

## 目录结构

```
__snapshots__/
├── SessionBrowserSearchNav.test.tsx.snap  # 会话浏览器搜索导航快照
└── SessionBrowserStates.test.tsx.snap     # 会话浏览器状态快照
```
