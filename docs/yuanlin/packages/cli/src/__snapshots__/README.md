# __snapshots__ (packages/cli/src/)

## 概述

此目录为 `packages/cli/src/` 下的测试快照目录，用于存储测试运行时生成的快照文件，供 Jest 快照测试进行回归对比。

## 目录结构

```
__snapshots__/
└── nonInteractiveCli.test.ts.snap    # 非交互式 CLI 测试快照
```
