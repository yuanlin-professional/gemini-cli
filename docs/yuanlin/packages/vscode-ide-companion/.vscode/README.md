# packages/vscode-ide-companion/.vscode

## 概述

VS Code IDE Companion 扩展的项目级 VS Code 配置目录，包含专用于该扩展开发的调试配置和任务定义。为扩展开发者提供便捷的调试和构建工作流。

## 目录结构

```
.vscode/
├── launch.json   # 调试启动配置，定义了扩展开发和调试的运行方式（如扩展宿主窗口启动等）
└── tasks.json    # 任务配置，定义了扩展的编译、打包等开发任务
```
