# __snapshots__ (core/src/tools)

## 概述

此目录存放 `packages/core/src/tools/` 下工具模块的测试快照文件。包含文件读取工具（read-file）和 Shell 工具（shell）的测试快照，用于回归验证这些核心工具的输出是否符合预期。

## 目录结构

```
__snapshots__/
├── read-file.test.ts.snap
└── shell.test.ts.snap
```
