# __snapshots__ (core/src/tools/definitions)

## 概述

此目录存放 `packages/core/src/tools/definitions/` 下工具定义模块的测试快照文件。当前包含 `coreToolsModelSnapshots.test.ts` 的快照，用于回归验证核心工具的模型定义快照是否与预期一致，确保工具定义的稳定性。

## 目录结构

```
__snapshots__/
└── coreToolsModelSnapshots.test.ts.snap
```
