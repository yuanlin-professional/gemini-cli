# __snapshots__ (core/src/core)

## 概述

此目录存放 `packages/core/src/core/` 下核心模块的测试快照文件。当前包含 `prompts.test.ts` 的快照，用于回归验证核心提示词（prompts）模块的输出是否符合预期。

## 目录结构

```
__snapshots__/
└── prompts.test.ts.snap
```
