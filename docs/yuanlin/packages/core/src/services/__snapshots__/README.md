# __snapshots__ (core/src/services)

## 概述

此目录存放 `packages/core/src/services/` 下服务层模块的测试快照文件。当前包含 `toolOutputMaskingService.test.ts` 的快照，用于回归验证工具输出脱敏服务（toolOutputMaskingService）的处理结果是否符合预期。

## 目录结构

```
__snapshots__/
└── toolOutputMaskingService.test.ts.snap
```
