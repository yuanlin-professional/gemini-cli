# __fixtures__ (core/src/utils)

## 概述

此目录存放 `packages/core/src/utils/` 下工具函数测试所需的固定测试数据文件（fixtures）。当前包含一个 WebAssembly 二进制文件 `dummy.wasm`，用作测试中的模拟 WASM 模块。

## 目录结构

```
__fixtures__/
└── dummy.wasm
```
