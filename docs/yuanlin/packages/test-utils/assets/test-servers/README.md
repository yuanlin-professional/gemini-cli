# packages/test-utils/assets/test-servers

## 概述

测试服务器配置目录，存放用于模拟外部服务的 JSON 配置文件。这些配置定义了测试环境中模拟服务器的行为和响应数据，使测试可以在不依赖真实外部服务的情况下运行。

## 目录结构

```
test-servers/
└── google-workspace.json   # Google Workspace 模拟服务器配置，定义了 Google Workspace API 的模拟响应数据
```
